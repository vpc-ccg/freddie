#include "commandline_flags.h"
#include "dag_align.h"
using namespace std;

int main(int argc, char *argv[]) {
    // parse_flags(argc, argv);
    // print_flags();
    process_gene_test();
    return 0;
}
